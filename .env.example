# Server
NODE_ENV=development
PORT=3000

# Database (MariaDB / MySQL)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=nails_domicile

# Session
# Générer un secret sécurisé avec :  node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
SESSION_SECRET=

# Admin (utilisé par npm run seed)
ADMIN_EMAIL=admin@nailsbysarah.fr
ADMIN_PASSWORD=

# Email (SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
MAIL_FROM=noreply@nailsbysarah.fr

# Demo
DEMO_MODE=false

# App
APP_URL=http://localhost:3000
